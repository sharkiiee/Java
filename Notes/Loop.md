
**Length Property**
Length property will give you length of the array.

	System.out.println(arry.length);

### For Each Loop in java
For each loop is an easy way to traverse the elements of the array

	for(int element:Arr){
		Sout(element);
	}

